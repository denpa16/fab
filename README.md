# API Система опросов пользователей

##### Запуск в терминале
- git clone `https://github.com/denpa16/fab.git` 
- перейти в директорию app
- собрать образы docker и запуск контейнеров через команду `docker-compose up -d --build`
